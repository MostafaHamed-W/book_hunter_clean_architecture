import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF100B20);
const kTransitionDuration = Duration(milliseconds: 500);
const kFeaturebBox = 'featured_books';
const kNewestBox = 'newest_books';
